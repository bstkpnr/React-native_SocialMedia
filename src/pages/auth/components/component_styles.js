import { StyleSheet } from 'react-native'

const button_styles = StyleSheet.create({
  container: {},
  text: {}
})
const input_styles = StyleSheet.create({
  container: {
    backgroundColor: '#e0e0e0',
    padding: 10,
    margin: 10,
    borderRadius: 5
  }
})
export { button_styles, input_styles }
